abstract final class AppDurations {
  static const Duration tick = Duration(milliseconds: 16);
  static const Duration menuAnimation = Duration(milliseconds: 450);
  static const Duration pageAnimation = Duration(milliseconds: 420);
  static const Duration gameOverDisplay = Duration(seconds: 3);
}
